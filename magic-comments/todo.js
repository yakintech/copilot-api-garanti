

/**
 * 
 * @todo Implement the sortArrayByParity function that takes an array of integers and returns an array with the even integers followed by the odd integers.
 */

function sortArrayByParity(A) {
    //
}